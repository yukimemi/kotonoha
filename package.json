{
  "name": "kotonoha",
  "private": true,
  "workspaces": ["backend/crates/kotonoha-server/web"],
  "scripts": {
    "_comment": "`bun --filter` swallows stdout on Windows in some versions — use explicit cd && bun for reliable Vite output. The frontend lives under the kotonoha-server crate so `cargo publish` can bake the built dist/ into the binary via rust-embed.",
    "dev": "cd backend/crates/kotonoha-server/web && bun run dev",
    "build": "cd backend/crates/kotonoha-server/web && bun run build",
    "preview": "cd backend/crates/kotonoha-server/web && bun run preview",
    "setup:tts": "bun run scripts/setup-tts.ts"
  }
}
